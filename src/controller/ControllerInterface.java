package controller;

/**
 * Intermediary class that handles data transfers between any model and view implementations.
 * Receives ApplicationManager's data from model and sends it to the view, receives any data change
 * requests from view and sends them to the model to actually be implemented.
 */
public interface ControllerInterface {

}
